<!-- <div id="<%= dom_id comment %>">
  <p>
    <strong>Name:</strong>
    <%#= comment.name %>
  </p>

  <p>
    <strong>Body:</strong>
    <%#= comment.body %>
  </p>

  <p>
    <strong>User:</strong>
    <%#= comment.user_id %>
  </p>

</div>
 -->

 <div class="col-lg-8 mb-">
      <div class="card shadow">
            <div class="card-body">
                  <table>
                        <tr>  
                        <td><p><b>Name: </b></p></td>
                        <td><p><%= comment.name %></p></td>
                        </tr>
                        <tr>
                        <td><p><b>Comment: </b></p></td>
                        <td><p><%= comment.body %></p></td>
                        </tr>
                        <p><%= time_ago_in_words(comment.created_at) %></p>
                        <%if current_user && current_user.admin %>
                        <td><p> <%= link_to 'Delete',[comment.book,comment],class: "btn btn-danger" ,method: :delete, data: {cofirm: 'Are you sure?'} %></p></td>
                        <%end%>
                  </table>
            </div>
      </div>
</div>